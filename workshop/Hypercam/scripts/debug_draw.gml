//TODO: remove if it's still empty

var line = 0;
var offset = 15;

//var obj = instance_place( x, y+1, all);
//draw_debug_text(x-20, y+(offset*line++), " " + string());
//draw_debug_text(x-20, y+(offset*line++), "state " + get_state_name(state));
//draw_debug_text(x-20, y+(offset*line++), "window " + string(window));
//draw_debug_text(x-20, y+(offset*line++), "charge " + string(uhc_current_cd.cd_spin_meter));
//draw_debug_text(x-20, y+(offset*line++), "image_index " + string(image_index));
//draw_debug_text(x-20, y+(offset*line++), "state*anim " + string(state_timer * walk_anim_speed));
