//TODO: remove if it's still empty

var line = 0;
var offset = 16;

//draw_debug_text(x-20, y+(offset*line++), "free " + string(free));
//draw_debug_text(x-20, y+(offset*line++), "vsp " + string(vsp));